<Window x:Class="SleekPicker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SleekPicker"
        Width="430"
        Height="820"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        Loaded="OnLoaded"
        Deactivated="OnDeactivated"
        MouseLeave="OnMouseLeavePanel"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#CC1A202B" />
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#66FFFFFF" />
        <SolidColorBrush x:Key="TextBrush" Color="#FFF5F8FF" />
        <SolidColorBrush x:Key="SectionHeaderBrush" Color="#FF9CD4FF" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FF5FC9FF" />
        <SolidColorBrush x:Key="ItemHoverBrush" Color="#2FFFFFFF" />
        <SolidColorBrush x:Key="ItemPressedBrush" Color="#44FFFFFF" />

        <Style x:Key="WindowItemButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ItemBorder"
                                CornerRadius="10"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ItemHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ItemPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseWindowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Width" Value="20" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CloseBorder"
                                CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CloseBorder" Property="Background" Value="{DynamicResource ItemHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CloseBorder" Property="Background" Value="{DynamicResource ItemPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid PreviewMouseLeftButtonDown="OnPanelPreviewMouseLeftButtonDown">
        <Border x:Name="PanelSurface"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource PanelBorderBrush}"
                BorderThickness="1"
                CornerRadius="20"
                Opacity="0.9" />

        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <DockPanel>
                    <TextBlock Text="SleekPicker"
                               Foreground="{DynamicResource AccentBrush}"
                               FontSize="26"
                               FontWeight="SemiBold" />
                    <TextBlock x:Name="VersionText"
                               Foreground="{DynamicResource TextBrush}"
                               Opacity="0.75"
                               Margin="8,8,0,0"
                               DockPanel.Dock="Right" />
                </DockPanel>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,12,0,8">
                <ScrollViewer x:Name="WindowListScrollViewer" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="DesktopSectionsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{DynamicResource SectionHeaderBrush}"
                                               FontWeight="SemiBold"
                                               Margin="2,0,0,6" />
                                    <ItemsControl ItemsSource="{Binding Windows}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,0,0,4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Button Grid.Column="0"
                                                            Style="{StaticResource WindowItemButtonStyle}"
                                                            Tag="{Binding}"
                                                            Margin="0"
                                                            Click="OnWindowButtonClick">
                                                        <DockPanel LastChildFill="True">
                                                            <Border Width="20"
                                                                    Height="20"
                                                                    CornerRadius="5"
                                                                    BorderBrush="{DynamicResource PanelBorderBrush}"
                                                                    BorderThickness="1">
                                                                <Image Source="{Binding Icon}" Stretch="Uniform" />
                                                            </Border>
                                                            <TextBlock Text="{Binding Title}"
                                                                       Foreground="{DynamicResource TextBrush}"
                                                                       Margin="10,0,8,0"
                                                                       VerticalAlignment="Center"
                                                                       TextTrimming="CharacterEllipsis" />
                                                        </DockPanel>
                                                    </Button>
                                                    <Border Grid.Column="1"
                                                            Width="20"
                                                            Height="20"
                                                            CornerRadius="5"
                                                            BorderBrush="{DynamicResource PanelBorderBrush}"
                                                            BorderThickness="1"
                                                            Margin="6,0,0,0">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding ShortcutLabel}" Value="{x:Null}">
                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding ShortcutLabel}" Value="">
                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding ShortcutLabel}"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   FontSize="11"
                                                                   FontWeight="SemiBold"
                                                                   HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center" />
                                                    </Border>
                                                    <Button Grid.Column="2"
                                                            Style="{StaticResource CloseWindowButtonStyle}"
                                                            Tag="{Binding}"
                                                            ToolTip="Close window"
                                                            Click="OnCloseWindowButtonClick"
                                                            Content="X" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <TextBlock x:Name="EmptyStateText"
                           Text="No windows available."
                           Foreground="{DynamicResource TextBrush}"
                           Opacity="0.75"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed" />
            </Grid>

            <DockPanel Grid.Row="2">
                <TextBlock x:Name="LastUpdatedText"
                           Foreground="{DynamicResource TextBrush}"
                           Opacity="0.75" />
            </DockPanel>
        </Grid>
    </Grid>
</Window>
